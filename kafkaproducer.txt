package com.jpmis.kafka;

import java.util.Properties;
import org.apache.kafka.clients.producer.*;

public class SimpleProducer 
{
    public static void main( String[] args )
    {
        Properties props = new Properties();
        props.put("bootstrap.servers","localhost:9092");
        props.put("key.serializer", "org.apache.kafka.common.serialization.StringSerializer");
        props.put("value.serializer", "org.apache.kafka.common.serialization.StringSerializer");
        
        Producer<String, String> producer = new KafkaProducer<String, String>(props);
        
        for(int i=0; i<10; i++){
        	producer.send(new ProducerRecord<String, String> ("hello-kafka1", Integer.toString(i), "message sent fine -" + Integer.toString(i)));
        	System.out.println("message sent fine");        	
        }
        producer.close();
    }
}
v 0.9.0

producer-broker-consumer
topic - partition - offset

zookeeper-server-start.sh config/zookeeper.properties
kafka broker 
kafka-server-start.sh config/server.properties
kafka-topics.sh --zookeeper bdtcstr55n1:2181 --create --topic MyFirstTopic --partitions 2 --replication-factor 1
kafka-console-producer.sh --broker-list localhost:9092 --topic testTopic
kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic MyFirstTopic --zookeeper bdtcstr55n1

